<%= form_for(@lineitem2) do |f| %>
  <% if @lineitem2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lineitem2.errors.count, "error") %> prohibited this lineitem2 from being saved:</h2>

      <ul>
      <% @lineitem2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :store_id %><br />
    <%= f.number_field :store_id %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br />
    <%= f.number_field :order_id %>
  </div>
  <div class="field">
    <%= f.label :cart_id %><br />
    <%= f.number_field :cart_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
