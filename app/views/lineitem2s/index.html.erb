<h1>Listing lineitem2s</h1>

<table>
  <tr>
    <th>Store</th>
    <th>Order</th>
    <th>Cart</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lineitem2s.each do |lineitem2| %>
  <tr>
    <td><%= lineitem2.store_id %></td>
    <td><%= lineitem2.order_id %></td>
    <td><%= lineitem2.cart_id %></td>
    <td><%= link_to 'Show', lineitem2 %></td>
    <td><%= link_to 'Edit', edit_lineitem2_path(lineitem2) %></td>
    <td><%= link_to 'Destroy', lineitem2, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Lineitem2', new_lineitem2_path %>
